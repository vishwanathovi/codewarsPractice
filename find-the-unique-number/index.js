https://www.codewars.com/kata/55f81f9aa51f9b72a200002f/train/javascript

/* find the unqiue number from an array

    Parameters : numbers(array), an array of intergers
    return: uniqueInt(int), an unique number

*/

// test cases: 
print(findUnique([1,2,2,3,4,3,4]) === 1);
print(findUnique([1234]) === 1234);
print(findUnique([ 3, 5, 5, 4, 4, 3, 2, 2, 9 ])===9);

function findUnique(numbers) {
    // iterate through the array 
    // add counter 
    // sort the array
    // IF : if count is equal to 1 then return the integer as unique number
}