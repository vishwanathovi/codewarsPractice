https://www.codewars.com/kata/578aa45ee9fd15ff4600090d/train/javascript

/* given aray of numbers,sort ascending odd numbers but even numbers must be on their places

    Parameters : array(arr), an array of numbers
    return : sortedOddInt(array), sorted ascending odd numbers;
*/
// test cases 
print(sortArray([5, 3, 2, 8, 1, 4]), [1, 3, 2, 8, 5, 4])
print(sortArray([5, 3, 1, 8, 0]), [1, 3, 5, 8, 0])
print(sortArray([]),[])

function sortArray(array) {
    // iterate the array 
    // IF odd number push to new array and sort the array 
    // compare two arays and 
    // IF first array contains odd replace it with first odd numner in new array and repeat 
    // for consectuve odd numbers 
  }