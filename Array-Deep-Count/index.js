// count all the elements in an array including the nested one;

//Parameters: An array of elemets,(arrOfEle);
//Return: number of all elements in array,(totalElemet);

//test cases:
 print(deepCount([1,2,3]) === 3);
 print(deepCount([[1,2,3],[13,34,12],55,'fg']) === 10);

function deepCount(arrOfEle) {
    // count all elements in an nexted array
    // (if array contains another array of elements take array as single element and 
    // again take length of it and sum it up)
    // use of loop and condition
    // use of reduce method and conditions

}