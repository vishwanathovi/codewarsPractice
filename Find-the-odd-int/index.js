https://www.codewars.com/kata/54da5a58ea159efa38000836/train/javascript

/*Find the int which appers odd number of times

    Parameters: A(array), An array of integers;
    Return: oddInt(int), number 

*/
// test cases 
print(findOdd([1,2,3,4,2,3,4]) === 1);
print(findOdd([-1,-1,-1,4,4]) === -1);

function findOdd(A) {
   // Traverse though array
   // sort the array
   // IF consecutive element are same increment count and push the element and count ad key value pairs to empty array
   // Else iterate to next element and do same
    // Iterate the new array 
    // IF value of any key is ODD store it in an variable and return it  
  }